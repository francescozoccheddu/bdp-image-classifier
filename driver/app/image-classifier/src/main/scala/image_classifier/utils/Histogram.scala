package image_classifier.utils

import org.apache.spark.rdd.RDD
import org.apache.spark.ml.linalg.{Vector => MLVector}

object Histogram {

	def compute(data : RDD[Int], min : Int, max : Int) : MLVector = {


		???
	}

}